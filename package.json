{
  "name": "active-window-capture",
  "version": "0.1.0",
  "description": "OBS window capture retargeting based on active OS window with verified OBS WebSocket APIs.",
  "main": "dist/app.js",
  "scripts": {
    "dev": "ts-node src/app.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/app.js"
  },
  "dependencies": {
    "obs-websocket-js": "^5.0.7",
    "systeminformation": "^5.21.19"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
